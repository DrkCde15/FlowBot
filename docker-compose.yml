version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/data/flowbot.db
      - NEXT_TELEMETRY_DISABLED=1
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
